<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.limengxiang.breeze.model.dao.JobExecLogRepository">
    <insert id="insert">
        INSERT INTO brz_job_exec_log (job_id,context,status,exec_at,elapse,created_at)
        VALUES (#{jobId},#{context},#{status},#{execAt},#{elapse},#{createdAt})
    </insert>
</mapper>
